{
  "name": "osaron-portfolio",
  "version": "1.0.0",
  "description": "Static portfolio with Playwright tests",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "http-server": "^14.1.1"
  }
}
